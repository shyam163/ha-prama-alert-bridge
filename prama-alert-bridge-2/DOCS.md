# Prama Alert Bridge 2

This is a second instance of the Prama Alert Bridge, allowing you to monitor a second Prama camera independently.

All configuration options and functionality are identical to the primary Prama Alert Bridge. See the main add-on's documentation for full details.

**Important:** Use a different `sensor_name` than the first instance (default: `prama2`) to avoid MQTT topic conflicts.
